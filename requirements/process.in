--only-binary :all:

# Renovate changes only this direct public authority pin. The pip-compile manager
# regenerates requirements/process.txt before the managed adoption runner executes.
engineering-process==0.3.0
